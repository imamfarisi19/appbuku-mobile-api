<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:viewmodels="clr-namespace:AppBuku.TMobileFromWeb.ViewModels" 
             xmlns:models="clr-namespace:AppBuku.Models;assembly=AppBuku.Models"
             x:DataType="viewmodels:AddReviewViewModel"
             Title="{Binding Title}"
             x:Class="AppBuku.TMobileFromWeb.Views.AddReviewPage">

    <ContentPage.BindingContext>
        <viewmodels:AddReviewViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Padding="20">
            <StackLayout>
                <Label Text="Nama" />
                <Entry Text="{Binding ReviewById.Nama}" />
                <Label Text="Isi Ulasan" />
                <Entry Text="{Binding ReviewById.IsiReview}" />
                <Label Text="Peringkat Buku (1 - 5)" />
                <Entry Text="{Binding ReviewById.Rating}" Keyboard="Numeric"/>
                <Label Text="Buku ke : "/>
                <Label Text="{Binding ReviewById.BukuId}"/>
            </StackLayout>

            <StackLayout Padding="0">
                <Button Text="Kirim" Margin="0,10" Command="{Binding CmdKirim}" />
                <Button Text="Kembali" Command="{Binding CmdBatal}"/>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>